{% set name = "code-server" %}
{% set version = "99.0.0-testing" %}

package:
  name: {{ name|lower }}
  version: {{ version|replace("-", "_") }}

source:
  url: https://github.com/cdr/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: e1fec09f56ece5fcbfb6fe71cd9baee4b5d092010c328a1814e121fd229d27f8
  patches:
    - node-binary.patch

build:
  number: 0
  binary_relocation: False  # [osx]
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - python 2.7.*
    - nodejs 10.*
    - yarn
    - pkg-config  # [linux]
  host:
    - nodejs 10.*
    - libxkbfile  # [linux]
    - xorg-libx11  # [linux]
    - icu
    - libuv
    - openssl
    - zlib
  run:
    - nodejs 10.*
    - icu
    - openssl
    - libuv
    - zlib

test:
  commands:
    - code-server --help

about:
  home: https://github.com/cdr/code-server
  license: MIT
  license_family: MIT
  license_file:
   - LICENSE
   - LICENSE.vscode.txt
   - ThirdPartyNotices.vscode.txt
  summary: 'Run VS Code on a remote server.'

extra:
  recipe-maintainers:
    - xhochy
